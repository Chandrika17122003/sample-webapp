pipeline {
    agent any

    stages {
        stage('Checkout Code') {
            steps {
                echo 'Checking out code from GitHub'
            }
        }

        stage('Test Pipeline') {
            steps {
                echo 'Jenkins pipeline is working successfully'
            }
        }
    }
}
